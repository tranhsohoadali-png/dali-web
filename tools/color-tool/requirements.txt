# Công cụ tách màu + mã DALI — dependencies cho Ubuntu/Linux
# Cài: pip install -r requirements.txt

Django==4.2.21
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0   # headless = không cần GUI (phù hợp server)
python-decouple>=3.8
extcolors>=1.0.0
shapely>=2.0.0                  # thay thế polylabelfast (Windows-only)
django-crispy-forms>=2.0
crispy-bootstrap5>=0.7
openpyxl>=3.1.0
asgiref>=3.7.0
sqlparse>=0.4.4
